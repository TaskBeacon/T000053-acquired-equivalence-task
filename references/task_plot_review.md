# Task Plot Review

- Flow: Diagram shows fixation, face/fish choice, training-only feedback, and ITI; transfer test omits feedback.
- Conditions: Diagram covers Stage 1 training, Stage 2 training, Stage 3 training, and Transfer test.
- Timing: Diagram uses 400 ms fixation, 3000 ms response window, 800 ms training feedback, and 400 ms ITI.
- Keys: Diagram shows `Z` for left fish and `M` for right fish.
- Criterion: Diagram notes Stage 1 and Stage 2 require 8 consecutive correct responses, Stage 3 requires 12, and training can repeat up to 8 attempts.
- README embed: `README.md` contains `![Task Flow](task_flow.png)` under `## 2. Task Flow`.
- Logo/header: Raw model output leaves blank header space; final `task_flow.png` is produced by `apply_taskbeacon_watermark.ps1`.
- Legacy cleanup: Removed old `references/plot_assets` task-flow helper images along with legacy spec/audit/source excerpt files.
