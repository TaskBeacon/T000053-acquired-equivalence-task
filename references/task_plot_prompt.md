# Task Plot Prompt

Create a clean scientific task-flow timeline diagram only.

Leave the top 25% of the image blank for a later fixed header, title, subtitle, and logo overlay. Do not draw any title, subtitle, logo, watermark, or branding.

Task: Acquired Equivalence Task.

Draw four horizontal timeline rows:

1. Stage 1 training
2. Stage 2 training
3. Stage 3 training
4. Transfer test

Use compact screen thumbnails connected by arrows. Use a white background, crisp dark labels, and restrained accent colors. Make row labels left aligned. Keep text legible and inside each timeline row.

Training stage rows:

- Fixation, 400 ms: fixation cross.
- Face + fish choice, 3000 ms: one simple face icon above two fish icons, lower left and lower right; show keys `Z=left` and `M=right`.
- Training feedback, 800 ms: Correct / Incorrect / Too slow.
- ITI, 400 ms: fixation cross.

Transfer test row:

- Fixation, 400 ms.
- Face + fish choice, 3000 ms: face cue and two fish options; show keys `Z=left` and `M=right`.
- No feedback.
- ITI, 400 ms.

Add small notes: Stage 1 criterion 8 consecutive correct; Stage 2 criterion 8; Stage 3 criterion 12; training stages may repeat up to 8 attempts; transfer test uses no feedback.

Do not include file names or implementation details.
